arquivo js -> instancia classe controller

classe controller

construtor

atributo local(formato da dats)
atributo display(tela da calculadora)
atributo data
atributo hora
atributo dataAtual(instancia de Date())
função inicializador
função eventos dos botoes

função inicialização
	intervalo (
		data -> instancia de Date()->formato string ->local
		hora -> instancia de Date()->formato string ->local{
			dia-> 2 digitos
			mes-> long
			ano-> numerico
		}

	)->1000ms

função varios eventos no mesmo botao

	recebe o elemento botao
	recebe os eventos
	recebe a função

	transforma os eventos em um array
	para cada elemento do array adiciona um evento
	recebendo o nome do evento e a função e inicializa com false

função evento dos botoes
	botoes -> seleciona id botoes
	botoes -> percorre(botoes, indexs)
	botoes -> adiciona nossa função
		(varios eventos no mesmo botao)
	recebe o botao, os eventos e adiciona uma função
	

pega data
salva data

pega hora 
salva hora

salva tela da calculadora

pega instancia de Date()

salva instancia de Date()